apiVersion: v1
kind: Pod
metadata:
  name: annotations
  annotations:
    buildurl: "https://jenkins.com/expense/job/backend/23"
    imageregistry: "https://hub.docker.com/"
    
spec:
  containers:
  - name: nginx
    image: nginx